import React, {FC} from "react";
import { FullScreenProps } from "./FullScreenProps";

const FullScreen:FC<FullScreenProps> = (props:FullScreenProps) =>{
    return(
        <div>
        
        </div>
    )
}
export default FullScreen;